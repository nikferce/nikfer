name: Supabase Ping

on:
  schedule:
    - cron: '0 9 * * 1'  # Her Pazartesi saat 09:00 UTC
  workflow_dispatch:       # Elle de çalıştırılabilir

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s "https://siszlrwhqpjjfxsuzlqa.supabase.co/rest/v1/" \
            -H "apikey: sb_publishable_L69LSIblq5UG3M1yrPxDyg_di4f2kz6" \
            -o /dev/null -w "HTTP %{http_code}"
